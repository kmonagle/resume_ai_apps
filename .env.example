ANTHROPIC_API_KEY=
VOYAGE_API_KEY=
# Neon connection string, e.g. postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require
DATABASE_URL=
# Shared password for the web UI (browser login prompt; username can be anything).
# Leave empty on localhost to skip it. Required when deployed on Render.
APP_PASSWORD=
